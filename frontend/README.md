# Frontend

The server-rendered UI lives in `templates/` and the shared styles/scripts live in `static/`.

This folder is reserved for future componentized frontend work if the dashboard later moves to a dedicated SPA build.
